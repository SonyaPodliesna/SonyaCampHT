import { ColorCheckDirective } from './color-check.directive';

describe('ColorCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorCheckDirective();
    expect(directive).toBeTruthy();
  });
});
